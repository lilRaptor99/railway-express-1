const React = require('react');
const { View, Text } = require('react-native');

export default function StationSchedule({ navigation }) {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Station Schedule page</Text>
    </View>
  );
}
